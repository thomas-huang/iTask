# itask runtime dependencies
# This package has no external runtime dependencies - uses only Python standard library

# For development and testing, install with: pip install -e .[dev]
# Or use requirements.dev.txt: pip install -r requirements.dev.txt